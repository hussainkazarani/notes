## For MacOS
1 &rarr; delete all old node and `.zshrc` links and update with `source ~/.zshrc`<br>
2 &rarr; run the command `brew install nvm`<br>
3 &rarr; the below are the important commands:
```bash
nvm on
nvm install 18.13.0
nvm use 18.13.0
nvm alias default 18.13.0
nvm uninstall 18.13.0
```